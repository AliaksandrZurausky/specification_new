# Проект: [НАЗВАНИЕ ПРОЕКТА]

## Описание проекта
[Краткое описание — что делает корпоративный портал, для кого, сколько пользователей]

---

## Стек и окружение

- **Продукт**: Bitrix24 Корпоративный портал (последняя версия)
- **PHP**: 8.5 (стабильная, актуальная на 2026 год; минимум 8.1)
- **БД**: MySQL 8.4 LTS (рекомендуемая LTS-версия на 2026 год)
- **Сервер**: [Apache / Nginx]
- **ОС сервера**: Linux

---

## Структура проекта

```
/                          — корень портала
├── /bitrix/               — ядро Bitrix (НЕ ИЗМЕНЯТЬ)
├── /local/                — все кастомные разработки
│   ├── /modules/          — кастомные модули
│   ├── /components/       — кастомные компоненты
│   ├── /templates/        — шаблоны
│   │   └── /[ИМЯ_ШАБЛОНА]/
│   ├── /php_interface/    — init.php, обработчики событий
│   └── /ajax/             — AJAX-обработчики
├── /upload/               — загружаемые файлы (не трогать)
└── [ДРУГИЕ ПАПКИ ПРОЕКТА]
```

---

## Правила разработки

### Обязательно
- Всю кастомную разработку вести только в `/local/` — никогда в `/bitrix/`
- Использовать **ORM D7** (`\Bitrix\Main\ORM`) для работы с БД
- Использовать **namespace** в кастомных модулях
- Подключать модули через `Loader::includeModule('...')` перед использованием
- Работать с файловой системой через `\Bitrix\Main\IO\*`
- Когда добавляешь использование какого-то метода, сперва его проверь, будет ли он работать
- Если есть какие-то вопросы, сперва ведём обсуждение, решаем все нюансы и только когда будет ясна полная картина по реализации, приступаем к написанию кода
- Если настроен mcp с гитхаб или есть git, то после глобальных правок делаем коммиты. Коммиты делаем по комплексу работ, а не по каждому файлу. Название комита должно отражать то, что было сделано

### Запрещено
- Прямые SQL-запросы без ORM (исключение: сложные запросы, которые нельзя выразить через ORM)
- Изменять файлы в `/bitrix/`
- Использовать устаревший API (`CIBlockElement`, `CUser` и т.п.) в новом коде — только при крайней необходимости для совместимости
- Начать делать код без обсуждения всех нюансов

### Предпочтительный стиль
- PSR-4 автозагрузка в модулях
- Обработчики событий — через `EventManager` в `init.php` или в `module.php`
- Кеширование через `\Bitrix\Main\Data\Cache` / тегированный кеш `\Bitrix\Main\Data\TaggedCache`

---

## Ключевые модули Bitrix24 портала

Основные модули, которые используются в проекте:

- `intranet` — корпоративный портал (основной)
- `socialnetwork` — живая лента, рабочие группы
- `tasks` — задачи
- `calendar` — календарь
- `im` — мессенджер / чат
- `crm` — CRM (если используется)
- `iblock` — инфоблоки
- `disk` — диск, хранилище файлов
- `bizproc` — бизнес-процессы
- `workflow` — документооборот

---

## Инфоблоки проекта

[Заполните при необходимости]

| Название | ID | Тип | Назначение |
|----------|-----|-----|------------|
| [Название] | [ID] | [ТИП] | [Что хранит] |

---

## Кастомные модули

[Перечислите, если есть]

- `[vendor].[module]` — [что делает]

---

## AJAX и REST

- AJAX-обработчики: `/local/ajax/`
- [Используется REST API Bitrix24: `\Bitrix\Main\Engine\Controller`]
- [Или: компонентный AJAX через `$APPLICATION->RestartBuffer()`]
- [Или: роутинг через `\Bitrix\Main\Routing`]

---

## Бизнес-процессы / автоматизация

---

## Типичные задачи в этом проекте

    - Разработать функционал создания/рекдактирования спецификаций в битриксе
    - Добавить возможность массового назначения по спецификациям 
    - Добавить возможность создания новой спецификации, указав материалы, альтернативы
---

## Важные особенности / известные проблемы

[Заполните, если есть нюансы]

- [Например: используется нестандартная схема прав доступа]
- [Например: кастомный модуль авторизации]

---

## Что НЕ нужно делать

- Не предлагать рефакторинг кода, не связанного с задачей
- Не добавлять комментарии и docblock к неизменённым функциям
- Не менять рабочий код "для улучшения", если не просили
- Не предлагать решения через устаревший API, если есть D7-аналог
